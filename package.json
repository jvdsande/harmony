{
	"name": "root",
	"private": true,
	"scripts": {
		"watch": "npm run watch:server & npm run watch:persistence & npm run watch:logger & npm run watch:query & npm run watch:example",
		"watch:logger": "cd packages/logger && npm run watch",
		"watch:persistence": "cd packages/persistence && npm run watch",
		"watch:server": "cd packages/server && npm run watch",
		"watch:query": "cd packages/query && npm run watch",
		"watch:example": "cd apps/example && npm run dll && npm run dev-webpack",
		"dev": "cd apps/example && npm run dev-server"
	},
	"devDependencies": {
		"@dazzled/eslint-config-dazzled": "^1.0.1",
		"lerna": "^3.4.3"
	}
}
